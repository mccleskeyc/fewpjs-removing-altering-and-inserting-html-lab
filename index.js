/* removes main */
main.remove()

/* creates cons variable newHeader set to a newly created h1 element*/ 
const newHeader = document.createElement("h1");

/* sets newHeader id to victory */
newHeader.id = "victory"

/* is the champion message */
newHeader.innerHTML = '${name} is the champion!'